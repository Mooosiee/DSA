#include <bits/stdc++.h>
  using namespace std;

  int main() {

    int n ,m;
    cin >> n >> m;
    string com[m]; 
    for (int i = 0; i < m; ++i) {
        cin >> com[i];
    }
    vector<pair<int,int>> arr;
    for(int i = 0;i < n;i++){
      int x,y;
      cin >> x >> y;
      arr.push_back(make_pair(x,y));
    }
    for(auto it : arr){
      cout << it.first << " "<< it.second << endl;
    }
    

    return 0;

  }